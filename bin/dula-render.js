#!/usr/bin/env node
/**
 * Dula Render CLI
 * Usage: dula-render <episode-dir>
 *
 * Thin wrapper around generate_video.js.
 * When installed via npm, this allows story projects to run:
 *   npx dula-render .
 */
import '../generate_video.js';
